import React from 'react';
import LoginComponent from '../../components/login';

// Login component that renders the login functionality
const Login = () => {
  return (
    <div>
      {/* Main login component handling user authentication */}
      <LoginComponent />
    </div>
  );
};

export default Login;
